apiVersion: 1

providers:
  - name: 'VetClinic Dashboards'
    orgId: 1
    folder: 'VetClinic'
    folderUid: 'vetclinic'
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
