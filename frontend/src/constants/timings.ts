/**
 * Timing constants used across the application.
 * Centralizes magic numbers for debounce delays, intervals, and timeouts.
 */

/** Default debounce delay for search inputs (milliseconds) */
export const DEBOUNCE_DELAY_MS = 300;

/** Auto-refresh interval for waiting room (milliseconds) */
export const AUTO_REFRESH_INTERVAL_MS = 30000;

/** Toast notification duration (milliseconds) */
export const TOAST_DURATION_MS = 3000;

/** Animation duration for transitions (milliseconds) */
export const ANIMATION_DURATION_MS = 200;
